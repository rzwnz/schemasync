<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- Property for log directory, defaulting to /app/logs or a system/env var -->
    <property name="LOG_PATH" value="${LOG_PATH:-/app/logs}"/>

    <!-- Ensure the log directory exists when running in environments where this matters -->
    <springProfile name="!test">
        <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
        <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>

        <!-- Console appender -->
        <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>
                    %clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr([%t]){faint} %clr(%c{36}) - %m%n%wex
                </pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <!-- Rolling file appender -->
        <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <!-- File location: use LOG_PATH, ensure the directory is mounted/writable -->
            <file>${LOG_PATH}/application.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <!-- daily rollover, keep 30 days -->
                <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>30</maxHistory>
                <!-- optional total size cap: e.g., 1GB -->
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
    </springProfile>

    <!-- In 'test' profile, only console logging to keep tests clean -->
    <springProfile name="test">
        <appender name="ConsoleTest" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
        <root level="WARN">
            <appender-ref ref="ConsoleTest"/>
        </root>
    </springProfile>

    <!-- Log levels -->
    <root level="INFO">
        <!-- In non-test profiles, include both console and file -->
        <springProfile name="!test">
            <appender-ref ref="Console"/>
            <appender-ref ref="RollingFile"/>
        </springProfile>
    </root>

    <!-- Reduce verbosity for Spring framework logs if desired -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="liquibase" level="INFO"/>
    <logger name="org.testcontainers" level="WARN"/>

    <!-- Your application package -->
    <logger name="com.example.schemasync" level="DEBUG"/>
</configuration>
